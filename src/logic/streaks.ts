/*
 * Rachas (CLAUDE.md §3): por hábito (estricta), global (umbral del 80 %),
 * semanal (objetivo mínimo), récords y días congelados que ni suman ni rompen.
 * Se implementa en la Fase 3, con batería de tests amplia.
 * Solo funciones puras: sin React, sin I/O y sin Date.now() sin inyectar.
 */
export {}
