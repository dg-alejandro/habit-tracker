import { describe, expect, it } from 'vitest'

/*
 * Test de humo: comprueba que Vitest está operativo (ROADMAP.md, Fase 0).
 * La Fase 1 lo sustituye por los tests reales de logic/dates.ts.
 */
describe('vitest', () => {
  it('está configurado y ejecuta tests', () => {
    expect(1 + 1).toBe(2)
  })
})
