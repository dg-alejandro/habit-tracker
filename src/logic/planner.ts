/*
 * Lógica del planificador (CLAUDE.md §4): semana ISO, generación de tareas
 * desde plantillas y arrastre de no completadas al inbox siguiente.
 * Se implementa en la Fase 4, con tests.
 * Solo funciones puras: sin React, sin I/O y sin Date.now() sin inyectar.
 */
export {}
