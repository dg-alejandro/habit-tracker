import { PagePlaceholder } from '../components/ui/PagePlaceholder'

/* Gestión de hábitos: crear, editar, reordenar, archivar (CLAUDE.md §5.2). */
export function Habits() {
  return <PagePlaceholder title="Hábitos" phase="Fase 1" />
}
