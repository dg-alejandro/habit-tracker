import { PagePlaceholder } from '../components/ui/PagePlaceholder'

/* Pantalla de inicio: registro diario de hábitos (CLAUDE.md §5.1). */
export function DailyLog() {
  return <PagePlaceholder title="Registro diario" phase="Fase 1" />
}
