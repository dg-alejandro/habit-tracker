import { PagePlaceholder } from '../components/ui/PagePlaceholder'

/* Ajustes y datos: exportar/importar JSON, umbral, sesión (CLAUDE.md §5.5). */
export function Settings() {
  return <PagePlaceholder title="Ajustes" phase="Fase 2" />
}
